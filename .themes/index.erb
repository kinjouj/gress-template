<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>test</title>
  </head>
  <body>
    <% entries.each do |entry| %>
    <a name="article-link" href="<%= entry.permalink %>"><%= entry.title %></a>
    <% end %>
  </body>
</html>
