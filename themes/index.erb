<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>test</title>
  </head>
  <body>
    <% @posts.each do |post| %>
    <a name="article-link" href="<%= post.permalink %>"><%= post.title %></a>
    <% end %>
  </body>
</html>
